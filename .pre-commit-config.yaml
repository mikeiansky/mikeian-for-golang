# .pre-commit-config.yaml

repos:
  - repo: local
    hooks:
      - id: golangci-lint
        name: GolangCI-Lint Check
        entry: golangci-lint
        language: system
        types: [go]
        args: [run, --config=.golangci.yaml]